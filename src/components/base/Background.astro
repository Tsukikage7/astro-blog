
<style>
  html::before,
  html::after {
    content: "";
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    transition: opacity 0.25s ease-in-out;
    pointer-events: none;
  }

  
  html::before {
    background: hsl(var(--background));
    opacity: 1;
  }

  html.dark::before {
    opacity: 0;
  }

  
  html::after {
    background: hsl(var(--background));
    opacity: 0;
  }

  html.dark::after {
    opacity: 1;
  }
</style>
