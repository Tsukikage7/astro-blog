name = "astro-blog"
compatibility_date = "2024-01-01"

# Pages 项目配置
pages_build_output_dir = "dist"

[build]
command = "pnpm run build"

[build.upload]
format = "directory"
dir = "dist"

# 环境变量配置
[env.production]
# 生产环境变量可以在这里配置

[env.preview]
# 预览环境变量可以在这里配置
